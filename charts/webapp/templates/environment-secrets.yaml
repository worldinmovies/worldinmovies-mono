apiVersion: v1
kind: Secret
type: Opaque
metadata:
    name: {{ printf "%s-env-secrets" .Release.Name }}
    namespace: {{ .Release.Namespace }}
stringData:
    {{- range $key, $val := .Values.secrets }}
    {{ $key }}: {{ $val | quote }}
    {{- end }}
