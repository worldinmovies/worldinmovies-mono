apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: web
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    name: {{ .Release.Name }}
  type: ClusterIP