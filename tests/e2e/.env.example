# Local-only fixture values for the E2E Docker Compose stack (tests/e2e).
# These are deliberately low-entropy dummy defaults — generate your own
# values for anything shared or public.
VITE_WEBAPP_PORT=80
TMDB_API=
MEILI_KEY=local-dev-meili-key-change-me
MONGO_URL=mongo
MONGO_USER=devmongo
MONGO_PASSWORD=devmongo-pass
RABBITMQ_URL=rabbitmq
RABBITMQ_DEFAULT_USER=devmq
RABBITMQ_DEFAULT_PASS=devmq-pass
DJANGO_SECRET_KEY=dev-django-secret-key
